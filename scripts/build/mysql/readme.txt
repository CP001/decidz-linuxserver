this database is used for manage.decidz.com
run the following script to trash the existing DB and replace it with a clean one:
sudo mysql -uroot -pvnls83e94fulhsa < /var/www/decidz/gitrepo/decidz-linuxserver/scripts/build/mysql/manage.decidz.com.sql
